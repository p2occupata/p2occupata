# Aula P2.0 Occupata — Jekyll Configuration
title: "Aula P2.0 Occupata"
description: "Uno spazio autogestito all'Università della Calabria per studiare, incontrarsi e organizzarsi."
lang: it

# Collections
collections:
  eventi:
    output: true
    permalink: /eventi/:path/
    sort_by: data

# Default front matter
defaults:
  - scope:
      path: ""
      type: "eventi"
    values:
      layout: "evento"

# Build settings
markdown: kramdown
exclude:
  - Gemfile
  - Gemfile.lock
  - README.md
  - node_modules
  - vendor
